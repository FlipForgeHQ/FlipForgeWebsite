import fs from 'node:fs';

const read=path=>fs.readFileSync(path,'utf8');
const index=read('index.html');
const heroCss=read('assets/css/homepage-decision-hero-v1.css');
const filmCss=read('assets/css/homepage-hero-film-v1.css');
const mobileCss=read('assets/css/homepage-mobile-nav-v1.css');
const dealCss=read('assets/css/homepage-deal-or-decoy-v1.css');
const navJs=read('assets/js/homepage-v1.js');
const filmJs=read('assets/js/homepage-hero-film-v1.js');
const dealJs=read('assets/js/homepage-deal-or-decoy-v1.js');
const netlify=read('netlify.toml');
const heroPath='assets/images/flipforge-homepage-hero.webp';
const heroBytes=fs.readFileSync(heroPath);
const checks=[];
const check=(name,condition)=>checks.push({name,passed:Boolean(condition)});
const isWebP=buffer=>buffer.length>12&&buffer.subarray(0,4).toString('ascii')==='RIFF'&&buffer.subarray(8,12).toString('ascii')==='WEBP';
const headerBlock=route=>{
  const marker=`for = "${route}"`;
  const start=netlify.indexOf(marker);
  if(start<0)return '';
  const next=netlify.indexOf('[[headers]]',start+marker.length);
  return netlify.slice(start,next<0?netlify.length:next);
};

check('001 locked category remains CARD INTELLIGENCE',index.includes('CARD INTELLIGENCE'));
check('002 locked slogan remains exact',index.includes('Before you buy. Know Why.'));
check('003 old descriptor remains forbidden',!index.includes('CARD VALUE INTELLIGENCE'));
check('004 real locked logo asset is used',index.includes('assets/brand/flipforge-logo-horizontal.svg'));
check('005 hero leads with the locked promise',index.includes('<span>Before you buy.</span>')&&index.includes('<strong>Know Why.</strong>'));
check('006 hero explains the value in one sentence',index.includes('FlipForge checks whether the evidence behind a sports-card deal actually holds up.'));
check('007 hero primary CTA opens the proof',index.includes('decision-button-primary" href="#deal-or-decoy" data-ff-see-action>Try the Deal Check'));
check('008 hero secondary CTA requests beta',index.includes('decision-button-secondary" href="beta-application.html">Request Beta Access'));
check('009 hero uses verified WebP asset',index.includes(heroPath)&&fs.existsSync(heroPath)&&isWebP(heroBytes));
check('010 hero concept stays explicitly illustrative',index.includes('Animated homepage concept · illustrative example · not live market data'));
check('011 unfinished MP4 is not referenced',!index.includes('flipforge-how-it-works-30s.mp4'));
check('012 proof asks one explicit task',index.includes('<strong>Your only task:</strong>')&&index.includes('choose what you would do with this $349 listing.'));
check('013 proof follows the hero',index.indexOf('id="deal-or-decoy"')>index.indexOf('class="decision-hero"'));
check('014 all four customer decisions are available',['BUY','WATCH','VERIFY','PASS'].every(v=>index.includes(`data-ff-choice="${v}"`)));
check('015 proof exposes the evidence correction',index.includes('5 of 7 comparisons were invalid.')&&index.includes('24.0%')&&index.includes('2.3%'));
check('016 comparison detail is optional',index.includes('<details class="ff-deal-comparisons">')&&index.includes('See all 7 comparison checks'));
check('017 CTA has explicit scroll behavior',dealJs.includes('scrollIntoView')&&dealJs.includes("'#deal-or-decoy'"));
check('018 one choice immediately reveals the result',dealJs.includes('choiceStage.hidden=true')&&dealJs.includes('resultStage.hidden=false'));
check('019 interaction is responsive',dealCss.includes('.ff-deal-choice-layout')&&dealCss.includes('@media(max-width:760px)'));
check('020 private-beta boundary stays explicit',index.includes('Controlled Private Beta.'));
check('021 transaction and profit boundary stays explicit',index.includes('FlipForge does not guarantee profit or authorize transactions.'));
check('022 commercial launch plans remain navigable',index.includes('href="pricing.html">Launch Plans</a>'));
check('023 mobile menu markup remains accessible',index.includes('class="menu-toggle"')&&index.includes('aria-expanded="false"')&&index.includes('aria-controls="mobile-navigation"')&&index.includes('class="mobile-nav" id="mobile-navigation"')&&index.includes('class="backdrop" aria-hidden="true"'));
check('024 mobile menu behavior remains loaded',index.includes('<script src="assets/js/homepage-v1.js" defer></script>'));
check('025 mobile menu closes with Escape',navJs.includes("event.key==='Escape'"));
check('026 mobile menu traps keyboard focus',navJs.includes("event.key!=='Tab'"));
check('027 dedicated hero CSS is first-paint dependency',index.includes('assets/css/homepage-decision-hero-v1.css'));
check('028 dedicated mobile CSS is first-paint dependency',index.includes('assets/css/homepage-mobile-nav-v1.css'));
check('029 dedicated Deal or Decoy CSS is first-paint dependency',index.includes('assets/css/homepage-deal-or-decoy-v1.css'));
check('030 mobile hero remains single-column',heroCss.includes('@media(max-width:760px)')&&heroCss.includes('grid-template-columns:1fr'));
check('031 mobile navigation is responsive',mobileCss.includes('@media(max-width:760px)')&&mobileCss.includes('.menu-toggle{display:block}'));
check('032 reduced-motion navigation remains',mobileCss.includes('@media(prefers-reduced-motion:reduce)'));
check('033 proof has responsive presentation',dealCss.includes('@media(max-width:760px)')&&dealCss.includes('.ff-deal-choices'));
check('034 production metadata keeps canonical URL',index.includes('<link rel="canonical" href="https://goflipforge.com/">'));
check('035 structured data keeps locked slogan',index.includes('"slogan":"Before you buy. Know Why."'));
check('036 sitewide frame policy permits approved same-origin embeds',headerBlock('/*').includes('X-Frame-Options = "SAMEORIGIN"'));
check('037 homepage remains explicitly unframeable',headerBlock('/').includes('X-Frame-Options = "DENY"')&&headerBlock('/').includes("frame-ancestors 'none'"));
check('038 index route remains explicitly unframeable',headerBlock('/index.html').includes('X-Frame-Options = "DENY"')&&headerBlock('/index.html').includes("frame-ancestors 'none'"));
check('039 operator route remains explicitly unframeable',headerBlock('/operator-beta.html').includes('X-Frame-Options = "DENY"')&&headerBlock('/operator-beta.html').includes("frame-ancestors 'none'"));
check('040 animated hero film stylesheet is loaded',index.includes('assets/css/homepage-hero-film-v1.css')&&filmCss.includes('.ff-hero-film'));
check('041 animated hero film behavior is loaded',index.includes('assets/js/homepage-hero-film-v1.js')&&filmJs.includes('25000'));
check('042 animated hero respects reduced motion',filmCss.includes('@media(prefers-reduced-motion:reduce)')&&filmJs.includes('prefers-reduced-motion'));
check('043 hero film shows evidence transformation',index.includes('5 of 7 rejected')&&index.includes('The apparent bargain nearly disappears.')&&index.includes('FlipForge decision'));

const failures=checks.filter(item=>!item.passed);
console.log('FlipForge video-first homepage validation');
console.log(`PASSED: ${checks.length-failures.length}`);
console.log(`FAILED: ${failures.length}`);
for(const failure of failures)console.error(`FAIL | ${failure.name}`);
if(failures.length)process.exitCode=1;
