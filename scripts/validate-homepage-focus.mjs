import fs from 'node:fs';

const read=path=>fs.readFileSync(path,'utf8');
const index=read('index.html');
const heroCss=read('assets/css/homepage-decision-hero-v1.css');
const mobileCss=read('assets/css/homepage-mobile-nav-v1.css');
const dealCss=read('assets/css/homepage-deal-or-decoy-v1.css');
const processCss=read('assets/css/homepage-deal-live-process-v3.css');
const navJs=read('assets/js/homepage-v1.js');
const dealJs=read('assets/js/homepage-deal-or-decoy-v1.js');
const netlify=read('netlify.toml');
const heroPath='assets/images/flipforge-homepage-hero.webp';
const heroBytes=fs.readFileSync(heroPath);
const checks=[];
const check=(name,condition)=>checks.push({name,passed:Boolean(condition)});
const isWebP=buffer=>buffer.length>12&&buffer.subarray(0,4).toString('ascii')==='RIFF'&&buffer.subarray(8,12).toString('ascii')==='WEBP';
const headerBlock=route=>{
  const marker=`for = "${route}"`;
  const start=netlify.indexOf(marker);
  if(start<0)return '';
  const next=netlify.indexOf('[[headers]]',start+marker.length);
  return netlify.slice(start,next<0?netlify.length:next);
};

check('001 locked category remains CARD DECISION INTELLIGENCE',index.includes('CARD DECISION INTELLIGENCE'));
check('002 locked slogan remains exact',index.includes('Before you buy. Know Why.'));
check('003 old descriptor remains forbidden',!index.includes('CARD VALUE INTELLIGENCE'));
check('004 real locked logo asset is used',index.includes('assets/brand/flipforge-logo-horizontal.svg'));
check('005 hero leads with the locked promise',index.includes('<span>Before you buy.</span>')&&index.includes('<strong>Know Why.</strong>'));
check('006 hero explains the value in one sentence',index.includes('FlipForge checks whether the evidence behind a sports-card deal actually holds up.'));
check('007 first interaction is already in the hero',index.includes('class="ff-deal-demo" id="deal-or-decoy"')&&index.includes('Would you pay <span>$349</span> for this card?'));
check('008 hero no longer requires a scroll CTA',!index.includes('Try the Deal Check')&&!index.includes('data-ff-see-action'));
check('009 hero metadata uses verified WebP asset',index.includes(heroPath)&&fs.existsSync(heroPath)&&isWebP(heroBytes));
check('010 example stays explicitly illustrative',index.includes('Illustrative example · not live market data'));
check('011 unfinished MP4 is not referenced',!index.includes('flipforge-how-it-works-30s.mp4'));
check('012 proof asks one explicit task',index.includes('YOUR ONLY TASK')&&index.includes('Pick what you would do. FlipForge will show you what changes.'));
check('013 all four customer decisions are available',['BUY','WATCH','VERIFY','PASS'].every(v=>index.includes(`data-ff-choice="${v}"`)));
check('014 proof exposes the evidence correction',index.includes('5 of 7 comparisons were invalid.')&&index.includes('24.0%')&&index.includes('2.3%'));
check('015 evidence detail is optional and modal',index.includes('<dialog class="ff-evidence-dialog"')&&index.includes('data-ff-open-evidence>See the evidence'));
check('016 no forced scrolling remains',!dealJs.includes('scrollIntoView'));
check('017 one choice enters the live processing stage',dealJs.includes("demo.dataset.ffState='processing'")&&dealJs.includes('processingStage.hidden=false'));
check('018 live sequence shows identity evidence value and decision',index.includes('01 · EXACT CARD')&&index.includes('02 · CHALLENGE 7 COMPARISONS')&&index.includes('03 · SUPPORTED VALUE')&&index.includes('04 · DECISION'));
check('019 live sequence rejects weak comparisons visibly',dealJs.includes("setCompState(index,'reviewing')")&&dealJs.includes("comp.dataset.finalState||'rejected'"));
check('020 live sequence recalculates to governed values',dealJs.includes("supportedValue.textContent='$357.20'")&&dealJs.includes("supportedDiscount.textContent='2.3%'"));
check('021 result follows processing in the same component',dealJs.includes('schedule(token,3820,showResult)')&&dealJs.includes('resultStage.hidden=false'));
check('022 interaction uses progressive enhancement',dealJs.includes('document.startViewTransition')&&dealJs.includes('showModal'));
check('023 interaction is responsive',dealCss.includes('@media(max-width:760px)')&&dealCss.includes('.ff-deal-choices')&&processCss.includes('@media(max-width:760px)'));
check('024 private-beta boundary stays explicit',index.includes('Controlled Private Beta.'));
check('025 transaction and profit boundary stays explicit',index.includes('FlipForge does not guarantee profit or authorize transactions.'));
check('026 commercial launch plans remain navigable',index.includes('href="pricing.html">Launch Plans</a>'));
check('027 mobile menu markup remains accessible',index.includes('class="menu-toggle"')&&index.includes('aria-expanded="false"')&&index.includes('aria-controls="mobile-navigation"')&&index.includes('class="mobile-nav" id="mobile-navigation"')&&index.includes('class="backdrop" aria-hidden="true"'));
check('028 mobile menu behavior remains loaded',index.includes('<script src="assets/js/homepage-v1.js" defer></script>'));
check('029 mobile menu closes with Escape',navJs.includes("event.key==='Escape'"));
check('030 mobile menu traps keyboard focus',navJs.includes("event.key!=='Tab'"));
check('031 dedicated hero CSS is first-paint dependency',index.includes('assets/css/homepage-decision-hero-v1.css'));
check('032 dedicated mobile CSS is first-paint dependency',index.includes('assets/css/homepage-mobile-nav-v1.css'));
check('033 dedicated Deal Check CSS is first-paint dependency',index.includes('assets/css/homepage-deal-or-decoy-v1.css'));
check('034 live-process CSS is loaded',index.includes('assets/css/homepage-deal-live-process-v3.css'));
check('035 desktop hero owns one dynamic viewport',heroCss.includes('min-height:calc(100dvh - 79px)'));
check('036 mobile hero remains single-column',heroCss.includes('@media(max-width:760px)')&&heroCss.includes('grid-template-columns:1fr'));
check('037 mobile navigation is responsive',mobileCss.includes('@media(max-width:760px)')&&mobileCss.includes('.menu-toggle{display:block}'));
check('038 reduced-motion navigation remains',mobileCss.includes('@media(prefers-reduced-motion:reduce)'));
check('039 mobile Deal Check keeps four choices compact',dealCss.includes("content:'START HERE · PICK ONE'")&&dealCss.includes('grid-template-columns:repeat(2,minmax(0,1fr))'));
check('040 production metadata keeps canonical URL',index.includes('<link rel="canonical" href="https://goflipforge.com/">'));
check('041 structured data keeps locked slogan',index.includes('"slogan":"Before you buy. Know Why."'));
check('042 sitewide frame policy permits approved same-origin embeds',headerBlock('/*').includes('X-Frame-Options = "SAMEORIGIN"'));
check('043 homepage remains explicitly unframeable',headerBlock('/').includes('X-Frame-Options = "DENY"')&&headerBlock('/').includes("frame-ancestors 'none'"));
check('044 index route remains explicitly unframeable',headerBlock('/index.html').includes('X-Frame-Options = "DENY"')&&headerBlock('/index.html').includes("frame-ancestors 'none'"));
check('045 operator route remains explicitly unframeable',headerBlock('/operator-beta.html').includes('X-Frame-Options = "DENY"')&&headerBlock('/operator-beta.html').includes("frame-ancestors 'none'"));
check('046 retired hero film CSS is still not loaded',!index.includes('assets/css/homepage-hero-film-v1.css'));
check('047 retired hero film JS is still not loaded',!index.includes('assets/js/homepage-hero-film-v1.js'));
check('048 choice processing and result stay in one component',index.includes('data-ff-choice-stage')&&index.includes('data-ff-processing-stage')&&index.includes('data-ff-result-stage'));
check('049 CTA to evaluate a real listing remains',index.includes('href="/app/#/evaluate" data-ff-deal-cta="evaluate_listing"'));
check('050 replay remains available without page travel',index.includes('data-ff-replay>Try another choice')&&dealJs.includes('showChoices()'));
check('051 evidence dialog can close natively',dealJs.includes('evidenceDialog.close'));
check('052 no browser storage is introduced',['localStorage','sessionStorage','indexedDB'].every(v=>!dealJs.includes(v)));
check('053 reduced motion is respected',heroCss.includes('@media(prefers-reduced-motion:reduce)')&&dealCss.includes('@media(prefers-reduced-motion:reduce)')&&processCss.includes('@media(prefers-reduced-motion:reduce)')&&dealJs.includes('prefers-reduced-motion'));
check('054 live processing is measured',dealJs.includes('flipforge_demo_processing_started'));

const failures=checks.filter(item=>!item.passed);
console.log('FlipForge first-viewport homepage validation');
console.log(`PASSED: ${checks.length-failures.length}`);
console.log(`FAILED: ${failures.length}`);
for(const failure of failures)console.error(`FAIL | ${failure.name}`);
if(failures.length)process.exitCode=1;
