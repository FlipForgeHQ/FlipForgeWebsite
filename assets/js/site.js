(()=>{const toggle=document.querySelector('.menu-toggle'),menu=document.querySelector('.mobile-nav'),backdrop=document.querySelector('.backdrop');let lastFocus=null;const focusable=()=>menu?[...menu.querySelectorAll('a,button,[tabindex]:not([tabindex="-1"])')]:[];const closeMenu=()=>{if(!toggle||!menu||!backdrop)return;toggle.setAttribute('aria-expanded','false');toggle.setAttribute('aria-label','Open navigation menu');menu.classList.remove('open');backdrop.classList.remove('open');backdrop.setAttribute('aria-hidden','true');document.body.classList.remove('menu-open');if(lastFocus)lastFocus.focus()};const openMenu=()=>{if(!toggle||!menu||!backdrop)return;lastFocus=document.activeElement;toggle.setAttribute('aria-expanded','true');toggle.setAttribute('aria-label','Close navigation menu');menu.classList.add('open');backdrop.classList.add('open');backdrop.setAttribute('aria-hidden','false');document.body.classList.add('menu-open');const items=focusable();if(items.length)items[0].focus()};if(toggle){toggle.addEventListener('click',()=>toggle.getAttribute('aria-expanded')==='true'?closeMenu():openMenu());backdrop.addEventListener('click',closeMenu);menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));document.addEventListener('keydown',e=>{if(e.key==='Escape')closeMenu();if(e.key==='Tab'&&menu.classList.contains('open')){const items=focusable();if(!items.length)return;const first=items[0],last=items[items.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}});window.addEventListener('resize',()=>{if(innerWidth>1000)closeMenu()})}document.querySelectorAll('.faq-item button').forEach(btn=>{btn.addEventListener('click',()=>{const item=btn.closest('.faq-item'),open=item.classList.toggle('open');btn.setAttribute('aria-expanded',String(open));btn.querySelector('span:last-child').textContent=open?'−':'+'})});const gradeForm=document.getElementById('grade-form');if(gradeForm){const n=id=>Math.max(0,Number(document.getElementById(id).value)||0),money=new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'});const update=()=>{const raw=n('raw-value'),cost=n('grading-cost'),vals=[n('psa10-value'),n('psa9-value'),n('psa8-value')],probs=[n('psa10-prob'),n('psa9-prob'),n('psa8-prob')],total=probs.reduce((a,b)=>a+b,0),status=document.getElementById('grade-status'),result=document.getElementById('grade-result');document.getElementById('prob-total').textContent=total.toFixed(0)+'%';if(Math.abs(total-100)>.01){status.textContent='Probabilities must total exactly 100% before FlipForge can evaluate the scenario.';result.textContent='CHECK INPUTS';result.className='badge red';return}const expected=vals.reduce((sum,v,i)=>sum+v*(probs[i]/100),0),net=expected-cost,gain=net-raw,rate=raw>0?(gain/raw)*100:0;document.getElementById('expected-value').textContent=money.format(expected);document.getElementById('net-value').textContent=money.format(net);document.getElementById('expected-gain').textContent=money.format(gain);if(rate>=20){result.textContent='GRADE CANDIDATE';result.className='badge green';status.textContent='The probability-weighted outcome shows meaningful expected upside after grading cost. Condition, time, fees, and market risk still require review.'}else if(gain>0){result.textContent='REVIEW';result.className='badge amber';status.textContent='Expected upside is positive but not strong enough to ignore condition, turnaround time, and selling fees.'}else{result.textContent='KEEP RAW';result.className='badge red';status.textContent='The probability-weighted outcome does not currently cover raw value and grading cost.'}};gradeForm.addEventListener('submit',e=>{e.preventDefault();update()});gradeForm.querySelectorAll('input').forEach(i=>i.addEventListener('input',update));update()}})();