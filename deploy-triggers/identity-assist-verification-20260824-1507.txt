Deploy identity-assist verification UX: distinguish possible matches from verified exact identities and allow a customer to re-check a visible review-only match without bypassing server-side identity verification.
