[build]
  command = "node scripts/build-assets.js"
  publish = "."

# FlipForge remains a static website. Netlify builds optimized visual assets,
# then publishes the repository root directly from the production branch.
