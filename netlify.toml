[build]
  command = "node scripts/build-assets.js && node scripts/validate-brand-assets.js"
  publish = "."

# FlipForge remains a static website. Netlify builds optimized visual assets,
# validates the approved brand system, then publishes the repository root.
