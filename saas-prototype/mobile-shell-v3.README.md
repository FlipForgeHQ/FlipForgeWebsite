# Mobile shell v3

Final mobile presentation owner for the authenticated customer app. It is imported after the existing customer layout system through `guided-mode-compact-v1.css` with cache-busted URLs so mobile Safari receives the current shell rules. Scope is presentation only.
